{
  "name": "checkout-service",
  "version": "1.0.0",
  "description": "Demo checkout microservice (Bun + Hono) with a planted observability bug for the Managed Agents talk",
  "type": "module",
  "scripts": {
    "dev": "bun run --watch src/index.ts",
    "start": "bun run src/index.ts",
    "test": "bun test"
  },
  "dependencies": {
    "hono": "^4.6.0"
  },
  "devDependencies": {
    "@types/bun": "^1.1.0"
  }
}
