{
  "name": "checkout-service",
  "version": "1.0.0",
  "description": "Demo checkout microservice (Node + Hono) with a planted observability bug for the Managed Agents talk",
  "type": "module",
  "scripts": {
    "dev": "node --watch --experimental-strip-types src/index.ts",
    "start": "node --experimental-strip-types src/index.ts",
    "test": "node --test --experimental-strip-types"
  },
  "dependencies": {
    "@hono/node-server": "^1.19.14",
    "hono": "^4.6.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0"
  }
}
